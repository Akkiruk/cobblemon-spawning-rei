name: Auto Labeler

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issues:
    types: [opened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  label-pr:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v6
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          configuration-path: .github/labeler.yml

  label-issue:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps:
      - name: Label issue based on title
        uses: actions/github-script@v7
        with:
          script: |
            const title = context.payload.issue.title.toLowerCase();
            const labels = [];
            
            if (title.includes('[bug]') || title.includes('crash') || title.includes('error')) {
              labels.push('bug');
            }
            if (title.includes('[feature]') || title.includes('request')) {
              labels.push('enhancement');
            }
            if (title.includes('neoforge') || title.includes('neo-forge')) {
              labels.push('neoforge');
            }
            if (title.includes('fabric')) {
              labels.push('fabric');
            }
            if (title.includes('rei') || title.includes('display')) {
              labels.push('rei');
            }
            if (title.includes('spawn')) {
              labels.push('spawning');
            }
            if (title.includes('evolution')) {
              labels.push('evolution');
            }
            
            if (labels.length > 0) {
              github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                labels: labels
              });
            }
